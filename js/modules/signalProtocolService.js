//import libsignal from "../vendor/libsignal-protocol"; // The tusted lib from openWhsiperSystem
//if (__DEV__) {
//import libsignal from "../vendor/libsignal";
//var libsignal = require("../vendor/libsignal");
// var signal = require("signal-protocol"); // we should make an audit with crypto expert of this lib
// or replace by the java and objective-c implemetation

//var KeyHelper = libsignal.KeyHelper;
//console.log(KeyHelper);

//  var registrationId = KeyHelper.generateRegistrationId();
// Store registrationId somewhere durable and safe.

//  KeyHelper.generateIdentityKeyPair().then(function(identityKeyPair) {
//    console.log(identityKeyPair);
// keyPair -> { pubKey: ArrayBuffer, privKey: ArrayBuffer }
// Store identityKeyPair somewhere durable and safe.
//  });

//}
